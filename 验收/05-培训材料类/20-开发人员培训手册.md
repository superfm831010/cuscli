# 开发人员培训手册

## 文档信息

| 项目名称 | 黄埔海关AI代码新技术应用服务项目 |
|---------|--------------------------------|
| 产品名称 | Cuscli AI编程助手平台 |
| 文档版本 | v1.0 |
| 编制日期 | 2025-11-01 |
| 编制单位 | 广州数研智能科技有限公司 |
| 保密等级 | 内部公开 |

---

## 修订历史

| 版本 | 日期 | 修订人 | 修订说明 |
|------|------|--------|---------|
| v1.0 | 2025-11-01 | 项目组 | 初始版本 |

---

## 目录

[TOC]

---

## 第1章 培训概述

### 1.1 培训目标

本培训旨在帮助开发人员掌握Cuscli AI编程助手平台的使用方法，提高代码开发效率和质量，将AI工具融入日常开发工作流程。

**培训后学员能够**：
- 熟练使用Cuscli进行代码生成
- 掌握代码检查和质量保障方法
- 将Cuscli集成到开发工作流
- 运用最佳实践提高开发效率
- 处理常见使用问题

### 1.2 培训对象

**适合人群**：
- 软件开发工程师
- 测试工程师
- 技术团队成员

**前置知识**：
- Python开发经验（推荐）
- 基本的Git使用
- 了解软件开发流程

### 1.3 培训大纲

| 章节 | 内容 | 建议时长 |
|------|------|---------|
| 第2章 | 快速入门 | 1小时 |
| 第3章 | 代码生成实践 | 2小时 |
| 第4章 | 代码检查实践 | 1.5小时 |
| 第5章 | Git集成应用 | 1小时 |
| 第6章 | 工作流集成 | 1小时 |
| 第7章 | 项目实战 | 2.5小时 |

**总建议培训时长**：9小时（约1.5天）

### 1.4 培训方式

**理论学习**：
- 功能特性介绍
- 使用技巧讲解
- 最佳实践分享

**动手实践**：
- 功能演示
- 实际操作练习
- 项目案例演练

**交流讨论**：
- 经验分享
- 问题解答
- 技巧交流

---

## 第2章 快速入门

### 2.1 第一次使用

#### 2.1.1 启动程序

```bash
# 进入项目目录
cd /path/to/your/project

# 启动Cuscli
auto-coder.chat
```

**启动成功标志**：
```
╔════════════════════════════════════════════════════════════╗
║        Cuscli AI编程助手 v1.1.0                            ║
╚════════════════════════════════════════════════════════════╝

AI模型: gpt-4o (已连接)
工作目录: /path/to/your/project
Git状态: main分支, 干净

请输入您的需求:
>
```

#### 2.1.2 基本交互

**查看帮助**：
```
> /help
```

**查看配置**：
```
> /config /show
```

**退出程序**：
```
> /exit
```
或按 `Ctrl+D`

### 2.2 5分钟体验

#### 2.2.1 生成第一段代码

**需求描述**：
```
> 请生成一个Python函数，计算两个数的最大公约数
```

**AI响应**：
```python
✓ 已生成并保存到: math_utils.py

def gcd(a, b):
    """
    计算两个数的最大公约数

    参数:
        a: 第一个整数
        b: 第二个整数

    返回:
        最大公约数
    """
    while b:
        a, b = b, a % b
    return a

✓ 单元测试已生成: tests/test_math_utils.py
```

#### 2.2.2 检查代码

```
> /check /file math_utils.py
```

**检查结果**：
```
✓ 代码规范检查通过
✓ 类型检查通过
✓ 安全检查通过
✓ 代码质量良好
```

#### 2.2.3 提交代码

```
> /git /commit
```

**自动生成提交信息**并完成提交。

### 2.3 界面和命令

#### 2.3.1 命令类型

**自然语言命令**：
```
> 请创建一个User类
> 帮我优化这个函数的性能
> 解释一下这段代码的作用
```

**斜杠命令**：
```
> /check /file src/main.py
> /git /status
> /help check
```

#### 2.3.2 常用快捷键

| 快捷键 | 功能 |
|--------|------|
| `↑` / `↓` | 浏览历史命令 |
| `Tab` | 命令补全 |
| `Ctrl+C` | 中断当前操作 |
| `Ctrl+D` | 退出程序 |
| `Ctrl+L` | 清屏 |

---

## 第3章 代码生成实践

### 3.1 生成函数和类

#### 3.1.1 生成简单函数

**场景**：需要一个数据验证函数

```
> 生成一个函数验证邮箱地址格式是否正确
```

**提示**：
- 明确输入输出类型
- 说明验证规则
- 指定返回值

**改进后的需求**：
```
> 生成一个Python函数validate_email，参数为字符串，
  返回布尔值表示邮箱格式是否正确，
  需要检查：
  - 包含@符号
  - @前后都有内容
  - 域名部分包含.
```

#### 3.1.2 生成类

**场景**：创建数据模型

```
> 创建一个User类，包含以下属性：
  - id: 用户ID
  - username: 用户名
  - email: 邮箱
  - created_at: 创建时间

  包含以下方法：
  - validate(): 验证数据有效性
  - to_dict(): 转为字典
  - from_dict(): 从字典创建对象

  使用类型注解，添加中文注释
```

#### 3.1.3 扩展已有代码

**场景**：基于现有代码扩展功能

```
> 基于 @src/models/base.py 中的 BaseModel，
  创建一个 ProductModel，添加以下字段：
  - name: 产品名称
  - price: 价格（Decimal类型）
  - category: 分类
  - stock: 库存数量
```

### 3.2 生成完整功能

#### 3.2.1 RESTful API开发

**需求描述**：
```
> 使用FastAPI实现用户管理的RESTful API，包括：

  GET    /api/v1/users         - 获取用户列表（支持分页）
  GET    /api/v1/users/{id}    - 获取单个用户
  POST   /api/v1/users         - 创建用户
  PUT    /api/v1/users/{id}    - 更新用户
  DELETE /api/v1/users/{id}    - 删除用户

  要求：
  - 使用Pydantic进行数据验证
  - 添加错误处理
  - 返回标准的JSON响应
  - 添加API文档注释
```

**生成内容**：
- API路由文件
- Pydantic schemas
- 业务逻辑层
- 单元测试

#### 3.2.2 数据库操作

**需求描述**：
```
> 使用SQLAlchemy实现用户数据的持久化，包括：
  - User模型定义
  - 数据库连接配置
  - CRUD操作封装
  - 事务处理

  数据库：PostgreSQL
  表名：users
```

### 3.3 代码优化和重构

#### 3.3.1 性能优化

**场景**：优化慢函数

```
> 请优化以下函数的性能，当前处理1000条数据需要5秒：

[贴入代码]

优化方向：
- 减少数据库查询次数
- 使用批量操作
- 添加缓存
```

#### 3.3.2 代码重构

**场景**：降低复杂度

```
> 请重构以下函数，降低复杂度：

[贴入代码]

要求：
- 提取子函数
- 减少嵌套层次
- 提高可读性
- 保持功能不变
```

### 3.4 测试代码生成

#### 3.4.1 单元测试

```
> 为 src/services/user_service.py 生成完整的单元测试，
  使用pytest框架，要求：
  - 覆盖所有public方法
  - 包含正常和异常情况
  - 使用fixture管理测试数据
  - 使用mock隔离外部依赖
```

#### 3.4.2 集成测试

```
> 生成用户注册流程的集成测试，测试：
  - API请求
  - 数据库保存
  - 邮件发送

  使用pytest和pytest-asyncio
```

### 3.5 生成技巧

#### 3.5.1 提供上下文

**技巧一：引用现有代码**
```
> 参考 @src/base_service.py 的实现方式，
  创建一个 ProductService
```

**技巧二：说明技术栈**
```
> 使用FastAPI + SQLAlchemy + PostgreSQL实现...
```

**技巧三：明确编码规范**
```
> 遵循PEP 8规范，使用类型注解，添加中文docstring
```

#### 3.5.2 逐步细化

**第一步：生成框架**
```
> 创建一个支付服务类，包含基本结构
```

**第二步：添加功能**
```
> 为支付服务添加支付宝支付方法
```

**第三步：完善细节**
```
> 添加支付状态查询和退款功能
```

**第四步：优化**
```
> 添加错误处理和日志记录
```

---

## 第4章 代码检查实践

### 4.1 检查单个文件

#### 4.1.1 基本检查

```bash
> /check /file src/api/users.py
```

**查看检查结果**：
- 通过项：符合规范的部分
- 警告项：建议改进的地方
- 错误项：必须修复的问题

#### 4.1.2 针对性检查

**仅检查安全问题**：
```bash
> /check /file src/api/users.py --only security
```

**仅检查性能问题**：
```bash
> /check /file src/services/data.py --only performance
```

### 4.2 检查多个文件

#### 4.2.1 检查目录

```bash
> /check /folder src/
```

**查看汇总报告**：
- 文件总数
- 通过/警告/错误统计
- 需要关注的文件列表

#### 4.2.2 检查Git修改

**检查未提交的修改**：
```bash
> /check /git /modified
```

**检查已暂存的文件**：
```bash
> /check /git /staged
```

### 4.3 自动修复

#### 4.3.1 交互式修复

```bash
> /check /file src/main.py --fix-interactive
```

**逐项确认修复**：
```
问题 1/5: SQL注入风险 (line 45)
当前代码:
  query = f"SELECT * FROM users WHERE id = {user_id}"

建议修复:
  query = "SELECT * FROM users WHERE id = %s"
  cursor.execute(query, (user_id,))

? 是否修复？[y/N/s/q]
  y - 修复
  N - 跳过
  s - 查看详细差异
  q - 退出
```

#### 4.3.2 批量修复

```bash
> /check /folder src/ --fix
```

**注意事项**：
- 修复前会自动备份
- 建议先用交互模式熟悉
- 修复后查看git diff确认改动

### 4.4 查看和导出报告

#### 4.4.1 查看报告

```bash
# 查看最新报告
> /report /show

# 列出所有报告
> /report /list

# 查看趋势
> /report /trend
```

#### 4.4.2 导出报告

```bash
# 导出Markdown
> /report /export markdown

# 导出JSON（用于CI/CD）
> /report /export json

# 导出HTML（便于分享）
> /report /export html
```

---

## 第5章 Git集成应用

### 5.1 日常Git工作流

#### 5.1.1 查看状态

```bash
> /git /status
```

**查看内容**：
- 当前分支
- 修改的文件
- 未跟踪的文件
- 与远程的差异

#### 5.1.2 智能提交

**自动生成提交信息**：
```bash
> /git /commit
```

**提交流程**：
1. AI分析修改内容
2. 生成符合规范的提交信息
3. 预览提交信息
4. 确认后提交

**生成的提交信息示例**：
```
feat: 实现用户认证功能

- 添加JWT token生成和验证
- 实现登录和注册接口
- 添加权限验证中间件
- 完善单元测试

测试状态: ✓ 所有测试通过
代码检查: ✓ 无问题
```

### 5.2 分支管理

#### 5.2.1 创建功能分支

```bash
> /git /branch /create feature/user-profile
```

**工作流程**：
```
main分支
  ↓
创建feature分支
  ↓
开发新功能
  ↓
提交代码
  ↓
创建PR
  ↓
合并到main
```

#### 5.2.2 合并分支

```bash
> /git /merge feature/user-profile
```

**冲突处理**：
- 自动检测冲突
- AI辅助解决冲突
- 手动确认解决结果

### 5.3 提交前检查

#### 5.3.1 自动检查

```bash
> /git /commit --check
```

**检查内容**：
1. 代码质量检查
2. 运行测试
3. 生成提交信息
4. 确认后提交

#### 5.3.2 检查工作流

**推荐流程**：
```bash
# 1. 查看修改
> /git /status

# 2. 检查代码
> /check /git /modified --fix

# 3. 运行测试
> /test

# 4. 提交
> /git /commit --check
```

### 5.4 Pull Request

#### 5.4.1 创建PR

```bash
> /git /pr /create
```

**自动生成**：
- PR标题
- 功能描述
- 改动列表
- 测试状态

#### 5.4.2 PR审查

```bash
# 查看PR详情
> /git /pr /show 123

# 检查PR代码
> /check /git /diff main..feature-branch
```

---

## 第6章 工作流集成

### 6.1 IDE集成

#### 6.1.1 VSCode集成

**使用方法**：
1. 在VSCode中打开终端
2. 启动Cuscli
3. 在终端中交互使用

**配置快捷键**（可选）：
```json
{
    "key": "ctrl+alt+c",
    "command": "workbench.action.terminal.sendSequence",
    "args": { "text": "auto-coder.chat\n" }
}
```

#### 6.1.2 PyCharm集成

**配置External Tool**：
1. Settings → Tools → External Tools
2. 添加新工具：
   - Name: Cuscli
   - Program: auto-coder.chat
   - Working directory: $ProjectFileDir$

### 6.2 命令行集成

#### 6.2.1 Shell脚本

**创建便捷脚本**：

```bash
#!/bin/bash
# check-and-commit.sh

# 检查代码
auto-coder.chat /check /git /modified --fix

# 运行测试
pytest

# 提交代码
auto-coder.chat /git /commit --check
```

#### 6.2.2 Makefile集成

```makefile
.PHONY: check commit

check:
	auto-coder.chat /check /folder src/

test:
	pytest tests/

commit: check test
	auto-coder.chat /git /commit --check
```

**使用**：
```bash
make check   # 代码检查
make commit  # 检查+测试+提交
```

### 6.3 CI/CD集成

#### 6.3.1 GitLab CI

```yaml
# .gitlab-ci.yml
stages:
  - check
  - test

code-check:
  stage: check
  script:
    - pip install auto-coder-1.0.39-py3-none-any.whl
    - auto-coder.chat /check /folder src/ --format json
  artifacts:
    reports:
      codequality: check-report.json

unit-test:
  stage: test
  script:
    - pytest tests/
```

#### 6.3.2 GitHub Actions

```yaml
# .github/workflows/check.yml
name: Code Quality Check
on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'

      - name: Install Cuscli
        run: pip install auto-coder-1.0.39-py3-none-any.whl

      - name: Run Code Check
        run: auto-coder.chat /check /folder src/
```

---

## 第7章 项目实战

### 7.1 实战一：开发RESTful API

#### 7.1.1 项目需求

**开发一个图书管理API**，包括：
- 图书的增删改查
- 分类管理
- 借阅记录
- 用户权限控制

#### 7.1.2 开发步骤

**步骤1：创建项目结构**
```
> 使用FastAPI创建项目结构，包含：
  - api/ 路由层
  - models/ 数据模型
  - schemas/ Pydantic schemas
  - services/ 业务逻辑
  - tests/ 测试
```

**步骤2：实现数据模型**
```
> 创建Book模型，包含字段：
  - id, title, author, isbn
  - category_id, status
  - created_at, updated_at

  使用SQLAlchemy ORM
```

**步骤3：实现API接口**
```
> 实现图书管理的RESTful API
```

**步骤4：代码检查**
```bash
> /check /folder src/
```

**步骤5：测试**
```
> 生成完整的单元测试和集成测试
```

**步骤6：提交代码**
```bash
> /git /commit --check
```

#### 7.1.3 实战要点

**使用Cuscli的技巧**：
- 逐模块开发，每个模块完成后检查
- 及时生成测试用例
- 利用Git集成管理代码
- 遇到问题请求帮助

### 7.2 实战二：代码重构

#### 7.2.1 场景描述

**接手一个遗留项目**，存在问题：
- 代码复杂度高
- 缺少测试
- 代码规范不统一
- 存在安全隐患

#### 7.2.2 重构步骤

**步骤1：代码分析**
```bash
> /check /folder src/
> /report /show
```

**步骤2：生成测试**
```
> 为现有代码生成单元测试，确保重构不改变功能
```

**步骤3：逐步重构**
```
> 请重构 src/payment.py，改进：
  - 降低函数复杂度
  - 提取重复代码
  - 添加错误处理
  - 统一命名规范
```

**步骤4：验证测试**
```bash
> /test
```

**步骤5：再次检查**
```bash
> /check /file src/payment.py
```

**步骤6：对比改进**
```bash
> /report /compare <before> <after>
```

### 7.3 实战三：团队协作

#### 7.3.1 场景描述

团队协作开发用户管理模块，需要：
- 统一代码规范
- 协同开发
- 代码审查

#### 7.3.2 协作流程

**步骤1：配置共享规则**
```bash
# 团队leader准备
> /rules /export team-rules.json

# 团队成员导入
> /rules /import team-rules.json
```

**步骤2：开发功能**
```bash
# 创建功能分支
> /git /branch /create feature/user-profile

# 开发功能...
# 代码检查
> /check /git /modified --fix

# 提交代码
> /git /commit --check
```

**步骤3：创建PR**
```bash
> /git /pr /create
```

**步骤4：代码审查**
```bash
# 审查者检查
> /check /git /diff main..feature/user-profile
```

**步骤5：合并代码**
```bash
> /git /pr /merge <pr-id>
```

---

## 第8章 高级技巧

### 8.1 提高效率的技巧

#### 8.1.1 使用别名

```bash
# 设置常用别名
> /alias ck = /check /file
> /alias cm = /git /commit --check

# 使用别名
> ck src/main.py
> cm
```

#### 8.1.2 保存常用会话

```bash
# 保存有价值的对话
> /save "用户认证实现"

# 加载已保存的会话
> /history /load "用户认证实现"
```

#### 8.1.3 使用模板

```bash
# 查看可用模板
> /template /list

# 使用模板快速创建项目
> /template /use restful-api
```

### 8.2 代码质量技巧

#### 8.2.1 持续检查

**建立检查习惯**：
- 编写完成后立即检查
- 提交前必须检查
- 定期全面检查

**检查策略**：
```bash
# 开发中 - 检查当前文件
> /check /file <current-file>

# 提交前 - 检查修改
> /check /git /modified

# 定期 - 全面检查
> /check /folder src/
```

#### 8.2.2 测试驱动

**TDD流程**：
```
1. 编写测试用例
   > 生成测试用例，测试用户登录功能

2. 实现功能代码
   > 实现用户登录功能

3. 运行测试
   > /test

4. 重构优化
   > 优化登录功能的性能
```

### 8.3 问题解决技巧

#### 8.3.1 请求代码解释

```
> 请解释以下代码的作用：
[贴入代码]
```

#### 8.3.2 错误诊断

```
> 这段代码运行出错，错误信息如下：
[贴入错误信息]

请帮我分析原因并提供修复建议
```

#### 8.3.3 性能优化

```
> 这个函数性能较差，请分析瓶颈并提供优化方案：
[贴入代码]
```

---

## 第9章 注意事项

### 9.1 使用建议

#### 9.1.1 合理使用AI

**AI擅长的**：
- 生成标准的CRUD代码
- 实现常见算法
- 代码格式化和规范检查
- 生成测试用例

**需要人工把关的**：
- 复杂业务逻辑
- 安全关键代码
- 性能优化决策
- 架构设计

#### 9.1.2 代码审查

**生成代码后务必**：
1. 仔细阅读和理解
2. 运行代码检查
3. 编写测试验证
4. 在开发环境测试
5. 代码审查

### 9.2 安全注意

#### 9.2.1 敏感信息

**不要将以下内容输入AI对话**：
- API密钥和密码
- 数据库连接字符串
- 用户隐私数据
- 商业机密信息

#### 9.2.2 代码安全

**关注安全问题**：
- SQL注入
- XSS攻击
- 敏感信息泄露
- 不安全的依赖

**利用代码检查**：
```bash
> /check /file <file> --only security
```

### 9.3 质量保证

#### 9.3.1 测试验证

**确保代码正确**：
- 编写充分的测试
- 覆盖正常和异常情况
- 进行集成测试
- 性能测试

#### 9.3.2 持续改进

**建立改进机制**：
- 记录使用经验
- 总结最佳实践
- 分享使用技巧
- 优化工作流程

---

## 第10章 考核评估

### 10.1 理论测试

**测试范围**：
- Cuscli功能理解
- 命令使用方法
- 工作流集成
- 最佳实践

**测试形式**：
- 选择题
- 简答题
- 案例分析

### 10.2 实操考核

**考核项目**：

| 考核项 | 内容 | 评分标准 |
|--------|------|---------|
| 代码生成 | 使用Cuscli生成功能代码 | 代码质量和完整性 |
| 代码检查 | 检查并修复代码问题 | 问题识别和修复 |
| Git集成 | 使用Git集成功能 | 操作正确性 |
| 项目实战 | 完成小型项目开发 | 综合应用能力 |

**及格标准**：
- 理论测试≥60分
- 实操考核≥3项合格
- 项目实战完成基本功能

### 10.3 证书颁发

**培训证书**：
- 完成全部培训内容
- 通过理论和实操考核
- 颁发《Cuscli开发人员培训合格证书》

---

## 附录A：常用命令速查

### A.1 代码生成

```bash
# 自然语言生成
> 创建一个User类...

# 基于现有代码
> 参考 @src/base.py 创建...

# 使用模板
> /template /use restful-api
```

### A.2 代码检查

```bash
# 检查文件
> /check /file src/main.py

# 检查目录
> /check /folder src/

# 检查Git修改
> /check /git /modified

# 自动修复
> /check /file src/main.py --fix
```

### A.3 Git操作

```bash
# 查看状态
> /git /status

# 智能提交
> /git /commit

# 提交前检查
> /git /commit --check

# 创建PR
> /git /pr /create
```

---

## 附录B：实用技巧集

### B.1 需求描述技巧

**好的需求描述包含**：
- 明确的功能要求
- 技术栈说明
- 输入输出定义
- 特殊要求

**示例**：
```
使用FastAPI实现用户注册接口，要求：
- 接收JSON格式的用户名、邮箱、密码
- 验证邮箱格式和密码强度
- 密码使用bcrypt加密
- 返回201状态码和用户信息（不含密码）
- 添加API文档注释
```

### B.2 调试技巧

**代码不work时**：
1. 描述问题和错误信息
2. 提供相关上下文
3. 说明期望结果
4. 请求具体修复

### B.3 学习技巧

**提高使用效果**：
- 多实践，积累经验
- 总结常用模式
- 与团队分享经验
- 持续学习新功能

---

## 附录C：项目案例

### C.1 案例1：用户管理系统

**需求**：完整的用户管理功能

**实现步骤**：
1. 数据模型设计
2. API接口实现
3. 单元测试编写
4. 集成测试
5. 代码检查和优化

**使用Cuscli的收获**：
- 节省开发时间
- 代码质量提高
- 测试覆盖完整

### C.2 案例2：代码质量改进

**场景**：提升代码质量

**改进过程**：
1. 全面代码检查
2. 识别问题
3. 自动修复部分问题
4. 手动优化复杂问题
5. 验证改进效果

**效果**：
- 代码质量得分提升
- 安全问题消除
- 可维护性增强

---

## 附录D：学习资源

### D.1 文档资源

- 快速入门指南
- 用户操作手册
- 命令参考手册
- 最佳实践指南
- 常见问题解答

### D.2 在线资源

- 培训视频
- 使用案例
- 技术文章
- 内部社区

### D.3 支持渠道

- 技术支持邮箱：support@example.com
- 内部工单系统
- 技术社区论坛
- 培训联系人

---

**文档编制**：广州数研智能科技有限公司
**版权所有**：© 2025 All Rights Reserved
**最后更新**：2025-11-01

**培训联系**：请联系项目组安排培训
